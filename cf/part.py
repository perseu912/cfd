import matplotlib.pyplot as plt
import numpy as np
import random as rd

t = np.linspace(0,10,100)

Np = 20

x,y = np.zeros(Np,1),np.zeros(Np,1)

for i in range(len(t)):
	for _ in range(Np):
		x[_] = 
